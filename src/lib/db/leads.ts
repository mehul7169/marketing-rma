/**
 * Phase 2 placeholder.
 *
 * This module is intentionally separate from the analytics modules so you can
 * add a future `leads` table + GHL webhook ingestion without refactoring the
 * existing dashboard pages.
 */

export type LeadsRow = {
  // TODO: define once the `leads` table exists in Supabase.
  id: string;
};

